function Switcher(labels) {
  return (
    // JSZ (React Element)
    // React.ReactNode JSX + null, string, number, array
    // object는(react의 child로 허용되지 않는다.)

    <button type="button"></button>
  );
}
